
<p style="color: green"><%= notice %></p>

<h2>List of ratings</h2>
<div id="ratings">
    <ul>
    <% @ratings.each do |rating| %>
    <li> <%= "#{Beer.find_by(id:rating.beer_id).name} #{rating.score}"  %> </li>
    <% end %>
    </ul>
</div>