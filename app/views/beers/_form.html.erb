<%= form_for(@beer) do |form| %>
  <% if beer.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(beer.errors.count, "error") %> prohibited this beer from being saved:</h2>

      <ul>
        <% beer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.label :name, style: "display: block" %>
  <%= form.text_field :name %>

  <%= form.label :style, style: "display: block" %>
  <%= form.select :style, options_for_select(@styles, :name) %>

  <%= form.label :brewery_id, style: "display: block" %>
  <%= form.select :brewery_id, options_from_collection_for_select(@breweries, :id, :name) %>

  <%= form.submit %>
 
<% end %>
